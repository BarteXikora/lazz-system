const config = {
    server: {
        PORT: '5000'
    },

    database: {
        host: 'localhost',
        database: 'lazz-system',
        user: 'root',
        password: ''
    }
}

module.exports = config